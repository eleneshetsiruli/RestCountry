import React from "react";
import { Filter } from "./Filter";

export const Header = () => {
  return (
    <header className="header">
      <p>Where in the world</p>
      <span>Dark Mode</span>
    </header>
  );
};
